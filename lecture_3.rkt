Higher-order functions

Scheme знае, че нещо е функция според ползването, тоест
дали стои (тук arg1 arg2 arg3).

branch изнася функции пред скоби.

d/dx : (R->R) -> (R->R)
Sdx : (R->R) -> (R->(R->R))


